import { DataTableTitle } from "react-native-paper/lib/typescript/src/components/DataTable/DataTableTitle";

export type Step = {
    screenName: string,
    icon: React.ReactElement
    title: string
    subtitle?: string
}